mutation addMovie($input: MovieInput!) {
    addMovie(input: $input) {
        id
        createdAt
        updatedAt
        title
        description
        reason
        poster
        rating
        creatorId
    }
}
