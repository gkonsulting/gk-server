query getMovies($limit: Int!, $cursor: String) {
    getMovies(cursor: $cursor, limit: $limit) {
        hasMore
        movies {
            id
            createdAt
            updatedAt
            title
            rating
            description
            reason
            poster
        }
    }
}
